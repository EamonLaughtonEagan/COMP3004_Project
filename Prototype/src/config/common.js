export default class common {
    static primary = "dodgerblue";
    static secondary = "#ff8d1e";
    static black = "#000";
    static gray = "#666";
    static softGray = "rgba(0, 0, 0, 0.2)";

    static MILLIS_MIN = 1000 * 60;
    static MILLIS_HOUR = this.MILLIS_MIN * 60;
    static MILLIS_DAY = this.MILLIS_HOUR * 24;
    static MILLIS_WEEK = this.MILLIS_DAY * 7;
    static MILLIS_MONTH = this.MILLIS_DAY * 30;
}
